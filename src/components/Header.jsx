function Header({ reactLogo,title }) {
  return (
    <header>
      {/* <img src={reactLogo} className="logo react" alt="React logo" /> */}
      <h1>{title}</h1>
    </header>
  );
}

export default Header;
